PORT=4000
CORS_ORIGIN=http://localhost:5173
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme123
# Used by the "Forgot password" form on /admin to reset ADMIN_PASSWORD without
# knowing it. If left unset, a random one is generated and printed to the
# server console on every restart.
ADMIN_RECOVERY_CODE=
